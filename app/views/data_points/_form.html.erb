<%= form_with(model: data_point, local: true) do |form| %>
  <% if data_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_point.errors.count, "error") %> prohibited this data_point from being saved:</h2>

      <ul>
      <% data_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data_point_collection_id %>
    <%= form.number_field :data_point_collection_id, id: :data_point_data_point_collection_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_area :value, id: :data_point_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
